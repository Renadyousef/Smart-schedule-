server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # ✅ This line fixes React Router reload crashes
  location / {
    try_files $uri /index.html;
  }

  # (Optional but good)
  error_page 404 /index.html;
}
